<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- title %></title>
</head>

<body>
    <h1>Search Results</h1>
    <% if(locals.users && locals.users.length>0){%>
        <% users.forEach((item)=>{ %>
            <p>User Id: <%= item.id %>
            </p>
            <p>User Name: <%= item.firstName %>
                    <%= item.lastName %>
            </p>
            <p>User Email: <%= item.email %>
            </p>
            <p>User Age: <%= item.age %>
            </p>
            <p>User Bio: <%= item.bio %>
            </p>

            <a href="/updateuser/<%= item.id %>">Click to Edit</a> <br>
            <form method="post" action="/delete/<%= item.id %>">
                <button type="submit"
                    onclick="return confirm('Are you sure you want to delete this user?')">Delete</button>
            </form>
            <hr />
            <% }) %>
                <% }else{ %>
                    <p>No Users found</p>
                    <hr />
                    <%}%>
</body>

</html>